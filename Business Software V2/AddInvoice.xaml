<Page
    x:Class="Business_Software_V2.AddInvoice"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Business_Software_V2"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="AddInvoice"
    d:DesignHeight="1080"
    d:DesignWidth="330"
    
    mc:Ignorable="d">

    <Grid Grid.Column="1" Width="330" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <!-- Header -->
            <RowDefinition Height="100"/>
            <!-- Drop Invoices -->
            <RowDefinition Height="50"/>
            <!-- Progress Bar -->
            <RowDefinition Height="30"/>
            <!-- History Label -->
            <RowDefinition Height="500"/>
            <!-- History Box -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20px">Add New Invoices</TextBlock>
        <Rectangle Grid.Row="1" Fill="LightGray"/>
        <TextBlock Grid.Row="1" FontSize="20px" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#B2000000">Drop Invoices Here</TextBlock>

        <ProgressBar x:Name="processingBar" Grid.Row="2" Margin="0 10" Height="30" VerticalAlignment="Top"></ProgressBar>
        <Label Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="15px">History</Label>
        <ListView Grid.Row="4" Margin="10">
            <ListViewItem>
                <StackPanel Height="30px"   >
                    <TextBlock FontWeight="Bold" FontSize="12px" Text="PDF.pdf"></TextBlock>
                    <TextBlock FontSize="10px" Text="23/04/1995"></TextBlock>
                </StackPanel>
            </ListViewItem>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Height="30px">
                        <TextBlock FontWeight="Bold" FontSize="12px" Text="{Binding FileName, FallbackValue='PDF.pdf'}"></TextBlock>
                        <TextBlock FontSize="10px" Text="{Binding Date, FallbackValue='23/04/1995'}"></TextBlock>
                    </StackPanel>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>



    </Grid>
</Page>
