<Page x:Class="Business_Software_V2.InvoiceList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Business_Software_V2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InvoiceList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label> Search </Label>
        <TextBox Margin="10 0"/>
        <ListView Name="listBoxInvoice" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Invoices}" Grid.Row="1" Margin="10 10">

            <ListView.View>
                <GridView AllowsColumnReorder="true"
              ColumnHeaderToolTip="Employee Information">

                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=DisplayName}" 
                      Header="File Name" Width="200"/>

                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=CompanyName}" 
                      Header="Company" Width="200"/>

                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=ABN}"  Header="ABN"
                      Width="100"/>


                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Date}" 
                      Header="Date" Width="70"/>

                    

                </GridView>
            </ListView.View>

            <ListView.ItemTemplate>
                <DataTemplate >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Name: " />
                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" />
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="ABN: " />
                            <TextBlock Text="{Binding ABN}" FontWeight="Bold" />
                        </StackPanel>
                        <Separator Grid.ColumnSpan="2"></Separator>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
